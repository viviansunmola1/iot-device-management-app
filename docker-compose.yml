version: '3'
services:
  frontend:
    build:
      context: ./frontend
    ports:
      - "3000:3000"  # Map container port 3000 to host port 3000
    depends_on:
      - backend

  backend:
    image: node:14  # You can use an official Node.js image
    working_dir: /app
    command: npm start
